/** @deprecated 请使用 `@/assets/icons` 中的 `APP_ICONS` 或 `AppIcon` 组件 */
export { getAppIconSrc, APP_ICONS, type AppIconType } from './index'

import { APP_ICONS } from './index'

/** @deprecated 请使用 `APP_ICONS['返回']` 或 `<AppIcon type="返回" />` */
export const NAV_BACK_ICON = APP_ICONS['返回']
