# 顾客订单

> **所属**：[订单模块](./订单模块.md)  
> **状态**：已迁入

## 范围

| 页面 / 能力 | 路径 | 状态 |
|-------------|------|------|
| 订单列表 | `pagesCustomer/order/list` | ✅ |
| 订单详情 · 取消订单 | `pagesCustomer/order/detail` | ✅（仅 `pending` 可取消；步骤条） |

## 前端

| 类型 | 路径 |
|------|------|
| 类型 | `src/types/order.ts` |
| 服务 | `src/services/order.ts` |

## 云端

- `order.get` / `order.list`
- `order.updateStatus` — 顾客取消（仅 `pending`）

## 已完成

- [x] 确认页提交后跳转详情
- [x] 列表 Tab、详情步骤条
- [x] 取消订单（仅待处理）+ 库存回滚

## 待办

- [ ] 微信支付接入后补充支付态展示
