# 角色与权限

> **所属**：[身份与登录模块](./身份与登录模块.md)  
> **状态**：已迁入

## 测试店长 OpenID

`oiDICxmmuGHJTKQzDsG9X32n2fAs` — 在 `login` 与各商家云函数白名单中，用于 B 端测试。

## 角色类型（云开发）

| 角色 | 标识 | 说明 |
|------|------|------|
| 店长 | `owner` | OpenID 白名单或 merchants 集合 |
| 管理员 | `manager` | merchants 集合 |
| 员工 | `staff` | merchants 集合 |
| 顾客 | — | 非 merchants 用户 |

其它微信登录默认为顾客；人员也可通过 `merchants` 集合授予商家权限。

## 客户端 Session

- `user_id` — 业务 userId；`hasToken()` 判断已登录
- `user_info` / `user_role` / `user_access_epoch`
- 本地**不含 openid**

## 云函数 `login`

| 场景 | action |
|------|--------|
| 微信一键登录 | `loginWechat` |
| 权限校验 | `checkAccess` |
| 读 / 存资料 | `getProfile` / `saveProfile` |
