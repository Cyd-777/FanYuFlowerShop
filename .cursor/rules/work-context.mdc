---
description: 维护 docs/🎛️ CURRENT.md；需求驱动；清单=项目交付项；完成项归档至 DONE
alwaysApply: true
---

# 工作上下文

- **收录范围**：只要是没有确定完成的，就要在里面；新出现的工作，也要填里面
- **归档**：[docs/🎛️ DONE.md](./docs/🎛️%20DONE.md) — 已完成交付项；**CURRENT 不显示、不用删除线**
- **三区布局**（`---` 只分隔三区，区内项目之间不加 `---`）：
  - **`## 正在做的`** — 一级 `[x]`
  - **`## 还没做的`** — 一级 `[ ]`
  - **`## 暂停的`** — 一级 `[ ] ~~`⏸` · 标题~~`
- **层级**：
  - **一级 = 项目** — 勾选仅表示正在做/没做/暂停；标题后 **`` `[████████] done/total` ``**
  - **二级 = `需求` · `清单` · `进度` · `下步可做`**
- **做完标准**：项目因**需求**提出；`需求` 已满足，且立项时 `清单` 里的交付项**全部做完**（未完成在 CURRENT，已完成在 DONE）→ 验收后整段移出面板
- **进度条**：`total` = 清单未完成项 + DONE 归档数；`done` = DONE 归档数
- **`需求`**：为何立项、要达成什么；验收时对照；不写实现步骤
- **`清单`**：项目交付项；立项时**一次写全**（可 **粗体分子组**）；CURRENT 只列未完成项
- **`进度`**：状态摘要；**不是**完成标准
- **`下步可做`**：当前焦点，通常是清单子集；**不是**完成标准
- **勾选**：`[x]` = 正在做 · `[ ]` = 没做（全局同时最多一项一级 `[x]`）
- **暂停**：~~`⏸` · 标题~~（只在「暂停的」区）
- **格式（勿改）**：
  - 一级：`- [x] · 项目名 `[████░░░░] 2/8``
  - 二级：`` `需求`：… `` · `` `清单` `` · `` `进度`：… `` · `` `下步可做` ``
- **措辞**：清单 **尽量用用户或 Issue 原句**；**16 段策略原话**只在 [功能实施策略.md §7](./docs/功能实施策略.md#7-项目策略原话归档16-段)
- **Cursor**：步骤完成 → 归档 DONE 并重算进度条；**新项目写全需求与清单**；验收通过 → 整段移入 DONE
- **维护频率（必做）**：`🎛️ CURRENT` 用来判断**当前正在做什么**；**每一次用户发起对话、且你在该轮里做了实质性工作**（写码、改配置、归档决策等），在**结束回复之前**必须同步维护 CURRENT / DONE——不是等多轮或等用户提醒。至少更新：相关项目的 `清单`（未完成留 CURRENT、完成移 DONE）、`进度`、`下步可做`、标题后进度条；跨项目影响（如 Issue #2、§7.x）一并改。无变更则不必改面板。
- **原提示词**：CURRENT 底部 YAML 固定，除非用户要求改规则
