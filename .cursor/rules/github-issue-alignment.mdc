---
description: GitHub Issue 需求对齐与决策清单回复规范（会话可选启用）
alwaysApply: true
---

# GitHub Issue 对齐规则

## 会话启用（新对话开始时）

在**新对话**中，若话题涉及需求规划、功能范围、Issue/议题、roadmap，或用户可能要通过 GitHub 对齐颗粒度，**先简短询问一次**：

> 本次对话是否启用 **GitHub Issue 对齐规则**？
> - **启用**：按下方格式读 Issue、在 Issue 下回复建议（含决策清单）
> - **不启用**：按普通对话处理，不主动走 Issue 流程

- 用户明确说「启用 / 执行 / 按 Issue 规则」→ 本会话启用
- 用户明确说「不启用 / 不用」→ 本会话不启用
- 用户直接给出 Issue 编号或链接（如「Issue #1」「读议题 3」）→ 视为启用，无需再问
- 本会话已确认过则不再重复询问
- 纯 bug 修复、单文件小改、与需求无关的技术问答 → 不必询问

## 启用后的工作流

1. 用 `gh issue view <编号>` 读取 Issue **最新正文与评论**
2. 在 Issue 下用 `gh issue comment` 回复（不在本地擅自改 Issue 正文，除非用户要求）
3. 用户根据决策清单勾选并**自行更新 Issue 文档**；下一轮基于更新后的正文继续

## 评论结构

每条评论包含：

1. **背景理解** — 复述需求，确认理解一致
2. **建议摘要** — 1～3 条结论性建议
3. **决策清单** — 每条可决策提议独立一组勾选框

## 决策清单格式（必须遵守）

```markdown
### 决策项 X：简短标题

（提议说明，含理由与影响范围。）

**请勾选一项：**
- [ ] 接受
- [ ] 不接受
- [ ] 有待改进
```

- 每条决策项只对应一个待决策点；多项拆成多条
- 用户勾选「有待改进」时，等待其补充后再出新一轮决策项
- 用户勾选「接受」后，可建议其将对应内容写入 Issue 正文，但不代写除非明确要求

## 工具与仓库

- GitHub 操作统一用 `gh` 命令
- 本仓库：`Cyd-777/FanYuFlowerShop`
- 功能总览参考：Issue #1（功能清单）
- **回复格式参考示例**：`.github/issue-reply-example.md`（勿在 Issue 内重复粘贴长模板）

## 完成度与「测试通过」

- **已实现**：代码链路完整，可在开发者工具或真机操作
- **测试通过**：仅指**你人工测试通过**；仓库无自动化测试，Agent 不得自行宣称某功能已测试通过
- 梳理进度时区分三态：**已实现 / 已实现待你测 / 未做**；只有你确认或 Issue 正文勾选后，才写「测试通过」
